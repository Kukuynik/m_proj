(function(){
  window.StatsPage = function StatsPage() {
    return `
      <div class="card">
        <h2>Статистика</h2>
        <p class="small">Локальная статистика пока не сохраняется. Можно добавить запись результатов в localStorage.</p>
      </div>
    `;
  };
})();